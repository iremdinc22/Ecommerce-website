@{
    ViewData["Title"] = "Şifre Değiştir";
}

<section class="d-flex justify-content-center align-items-center vh-100 bg-light">
    <div class="card shadow-lg p-4 rounded-4" style="max-width: 500px; width: 100%;">
        <div class="text-center mb-4">
            <i class="bi bi-shield-lock-fill text-primary" style="font-size: 3rem;"></i>
            <h2 class="mt-2">Şifre Değiştir</h2>
            <p class="text-muted">Yeni şifrenizi belirlemek için aşağıdaki formu doldurun.</p>
        </div>

        <form asp-action="PasswordChange" method="post">
            <!-- 🔒 Gizli user bilgisi (UserGuid) -->
            <input type="hidden" name="user" value="@Context.Request.Query["user"]" />

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label for="NewPassword" class="form-label">Yeni Şifre</label>
                <input type="password" class="form-control form-control-lg rounded-3" id="NewPassword" name="NewPassword" placeholder="Yeni şifre" required>
            </div>

            <div class="mb-3">
                <label for="ConfirmPassword" class="form-label">Yeni Şifre (Tekrar)</label>
                <input type="password" class="form-control form-control-lg rounded-3" id="ConfirmPassword" name="ConfirmPassword" placeholder="Yeni şifre tekrar" required>
            </div>

            <button type="submit" class="btn btn-success w-100 rounded-3 fw-semibold">Şifreyi Güncelle</button>
        </form>

        <div class="text-center mt-3">
            <a asp-controller="Account" asp-action="Login" class="text-decoration-none text-muted">
                <i class="bi bi-arrow-left-circle"></i> Giriş sayfasına dön
            </a>
        </div>
    </div>
</section>
